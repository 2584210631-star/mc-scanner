# MC Scanner 优化说明

## 优化版本
基于 mc-scanner v1 (13 commits) 优化，保留所有原有功能。

## 优化内容

### 1. Bug 修复
- **scanner.py**: 修复 `masscan_scan` 函数缺少 `import json` 导致的 NameError

### 2. 排除列表接入（main.py）
- `_load_targets` 函数现在自动加载 `exclude.conf` 并过滤目标
- 新增 `--exclude` 命令行参数，可覆盖配置文件中的排除列表
- `portscan`、`scan`、`warn` 三个命令均支持排除列表
- 过滤时会打印移除数量，便于确认

### 3. 协议多级回退增强（bot.py）
- SLP 探测改用 `probe_with_fallback()`，失败时自动尝试常见协议版本
- 登录层面新增协议回退循环：当某个协议版本连接失败时，自动尝试下一个支持的版本
- 候选协议列表构建逻辑：优先使用 SLP 探测到的版本，然后按 COMMON_PROTOCOLS 回退
- 每次尝试前重置 BotResult 状态字段，避免上一次失败的结果残留
- 全部失败时返回最后一个错误信息

### 4. 扫描结果自动入库（main.py）
- 新增 `_save_results_to_db()` 辅助函数
- `scan` 和 `warn` 命令完成后自动将结果写入 SQLite 数据库
- 支持 dict 格式（scan 结果）和 BotResult 格式（warn 结果）
- 新增 `--no-db` 参数可禁用自动入库
- 配置文件新增 `auto_save_db` 选项
- 入库失败不影响扫描结果，仅打印警告

### 5. 配置文件补全（config.json）
- 新增 `rate`: 每秒最大连接数（0=不限速）
- 新增 `authme_password`: AuthMe 密码（空=自动生成）
- 新增 `exclude_file`: 排除列表文件路径
- 新增 `db_path`: SQLite 数据库路径（null=默认）
- 新增 `auto_save_db`: 扫描结果自动入库开关
- `portscan` 命令新增 `--rate` 参数支持

### 6. 文档更新（README.md）
- 配置文件示例补充新配置项
- 常用参数表补充 `--exclude` 和 `--no-db`

## 文件变更清单
| 文件 | 变更类型 | 说明 |
|------|----------|------|
| scanner.py | Bug修复 | 补充 import json |
| main.py | 功能增强 | 排除列表接入、自动入库、新参数 |
| bot.py | 功能增强 | 协议多级回退（登录层面） |
| config.json | 配置补全 | 新增5个配置项 |
| README.md | 文档更新 | 补充配置项和参数说明 |

## 验证结果
- ✅ 所有 Python 文件语法检查通过
- ✅ 所有模块正常导入
- ✅ 7 个子命令正常识别
- ✅ 新参数（--exclude/--rate/--no-db）生效
- ✅ SQLite 插入/查询/统计功能正常
- ✅ 排除列表过滤功能正常
- ✅ 47 个常见协议版本全部支持
- ✅ bot 模块函数签名和字段完整

## 使用示例
```bash
# 带排除列表和限速扫描
python main.py scan 1.2.3.0/24 --exclude exclude.conf --rate 500

# 扫描结果不入库
python main.py scan 1.2.3.0/24 --no-db

# 警告模式带排除列表
python main.py warn 1.2.3.0/24 --exclude exclude.conf

# 查询数据库
python main.py query --auth offline
python main.py query --stats
```
